# tersawy.com — agent guide

The canonical agent guide is **`AGENTS.md`** — the single source of truth shared with every agent.
Claude Code pulls it in via the import below; if your tooling doesn't expand the import, open
`AGENTS.md` directly. This file carries no guidance of its own, so the two never drift.

@AGENTS.md
